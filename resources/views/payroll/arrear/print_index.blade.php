<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arrear Processes - Print</title>
    <style>
        @page {
            size: A4 portrait;
            margin: 15mm 10mm;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            font-size: 9pt;
            line-height: 1.3;
            color: #000;
            background: #fff;
        }

        .report-header {
            border-bottom: 3px solid #333;
            margin-bottom: 15px;
            padding-bottom: 10px;
        }

        .report-header h1 {
            font-size: 16pt;
            font-weight: bold;
            margin-bottom: 4px;
        }

        .report-header p {
            font-size: 8pt;
            color: #666;
            margin-bottom: 2px;
        }

        .report-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 12px;
            padding: 6px 8px;
            background: #f5f5f5;
            border: 1px solid #ccc;
            font-size: 8pt;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 15px;
        }

        thead {
            background-color: #ddd;
            border-bottom: 2px solid #333;
        }

        th {
            padding: 6px 4px;
            font-weight: bold;
            text-align: center;
            border: 1px solid #999;
            font-size: 8pt;
        }

        td {
            padding: 5px 4px;
            border: 1px solid #ccc;
            font-size: 8pt;
            vertical-align: middle;
            text-align: center;
        }

        tbody tr {
            page-break-inside: avoid;
        }

        tbody tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .text-left {
            text-align: left;
        }

        .badge {
            display: inline-block;
            padding: 1px 5px;
            border: 1px solid #666;
            font-size: 7pt;
        }

        .badge-approved { background: #d4edda; border-color: #c3e6cb; color: #155724; }
        .badge-pending  { background: #fff3cd; border-color: #ffeeba; color: #856404; }
        .badge-rejected { background: #f8d7da; border-color: #f5c6cb; color: #721c24; }

        .report-footer {
            margin-top: 20px;
            padding-top: 10px;
            border-top: 1px solid #ccc;
            font-size: 8pt;
            color: #666;
            display: flex;
            justify-content: space-between;
        }
    </style>
</head>

<body>

    <div class="report-header">
        <h1>Arrear Adjustment Processes</h1>
        <p>List of all processed arrear adjustment batch processes, total employees, amounts, and approval states</p>
        <p><strong>Report Date:</strong> {{ date('l, F j, Y') }}</p>
    </div>

    <div class="report-info">
        <div><strong>Total Batches:</strong> {{ count($records) }}</div>
        @if(request('keyword'))
            <div><strong>Keyword Filter:</strong> "{{ request('keyword') }}"</div>
        @endif
        <div><strong>Printed:</strong> {{ date('d M, Y h:i A') }}</div>
    </div>

    <table>
        <thead>
            <tr>
                <th style="width:5%;">#</th>
                <th style="width:25%;">Batch ID</th>
                <th style="width:15%;">Salary Month</th>
                <th style="width:15%;">Total Employees</th>
                <th style="width:15%;">Total Amount</th>
                <th style="width:15%;">Generated By</th>
                <th style="width:10%;">Status</th>
            </tr>
        </thead>
        <tbody>
            @foreach ($records as $index => $record)
                <tr>
                    <td>{{ $index + 1 }}</td>
                    <td class="text-left">
                        <strong>{{ $record->batch_id ?? 'N/A' }}</strong><br>
                        <span style="font-size:7pt; color:#666;">On: {{ $record->created_at ? $record->created_at->format('d M Y') : '—' }}</span>
                    </td>
                    <td>{{ $record->salary_month ? \Carbon\Carbon::parse($record->salary_month)->format('F Y') : '—' }}</td>
                    <td>{{ $record->total_employee ?? 0 }}</td>
                    <td>৳{{ number_format($record->total_amount ?? 0, 2) }}</td>
                    <td>{{ $record->generatedBy?->employee?->full_name ?? $record->generatedBy?->name ?? 'N/A' }}</td>
                    <td>
                        <span class="badge badge-{{ strtolower($record->approval_status ?? 'pending') }}">
                            {{ ucfirst($record->approval_status ?? 'Pending') }}
                        </span>
                    </td>
                </tr>
            @endforeach
        </tbody>
    </table>

    <div class="report-footer">
        <div><strong>Generated by HRMS</strong></div>
        <div>Page 1 of 1</div>
    </div>

    <script>
        window.onload = function() {
            window.print();
        };
    </script>

</body>

</html>
