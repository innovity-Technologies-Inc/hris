# Implementation Plan: Fix Missing Leave Module in Sidebar

## Objective
Correct the permission checks and active state logic in the sidebar to ensure the "Leaves" module is visible to users with the appropriate permissions.

## Key Files & Context
- `resources/views/structure/partials/sidebar.blade.php`
- `database/seeders/PermissionSeeder.php` (Context for correct slugs)

## Implementation Steps
1. **Fix Permission Slugs**: In `sidebar.blade.php`, update the permission checks for the Leaves module to use the pluralized slugs generated by the system (`leaves.create` and `leaves.view`).
2. **Update Active State Logic (Leaves)**: Update the `Route::is()` and `request()->is()` logic for the Leaves menu to accurately reflect the active route.
3. **Update Active State Logic (Movement)**: Apply the same robust `Route::is()` active state logic to the Movement menu.

## Verification & Testing
1. Manually verify the sidebar renders the Leaves module for an employee or admin user.
2. Verify the "active" highlight styling correctly applies when navigating through the Leaves and Movement pages.
